# CLAUDE.md

This file gives AI assistants (Claude Code and others) the context they need to work in this repository.

## Project status

`starprivacy` is a brand-new, empty repository — no application code, dependency manifests, or build
tooling have been committed yet. This file is a starting point; update it as real structure lands so it
stays a source of truth rather than stale scaffolding.

## Getting started (once code exists)

There is nothing to install, build, or test yet. When the first code is added to this repo, update this
section with the actual commands, for example:

- Install dependencies: _(add command, e.g. `npm install`, `pip install -r requirements.txt`)_
- Run the app locally: _(add command)_
- Run tests: _(add command)_
- Lint / format: _(add command)_
- Build for production: _(add command)_

## Conventions for future work

Until real conventions emerge from the codebase, default to these general practices:

- Prefer small, focused commits with clear messages.
- Keep this CLAUDE.md up to date whenever the project structure, tooling, or workflows change —
  it should always reflect the current state of the repo, not its history.
- Document any non-obvious architectural decisions here (or in a linked doc) as they're made, so
  future contributors and AI assistants don't have to rediscover them.

## Next steps for whoever documents this repo next

When this repository has actual content, revisit this file and fill in:

1. **Project purpose** — what `starprivacy` does and who it's for.
2. **Tech stack** — languages, frameworks, and major dependencies.
3. **Directory structure** — a map of top-level folders and what lives in each.
4. **Development workflow** — how to run, test, and debug locally; branching model; CI/CD.
5. **Key conventions** — code style, naming patterns, testing expectations, and any gotchas.
